CREATE MATERIALIZED VIEW "TEST_REGEXP_EXTRACT" 
AS
WITH input AS (
    SELECT 'SEARCH OVER THIS STRING: TO FIND THIS' as the_string
)
SELECT
    the_string,
    REGEXP_EXTRACT(the_string, 'TO FIND THIS') as extracted_text
FROM input;